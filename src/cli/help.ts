export function getHelpText(): string {
  return `pkg-guard

Guard npm package manifests, entry points, and release workflows before publishing.

Usage:
  pkg-guard --help

Commands will be implemented in the next phase.
`;
}
